import React from "react";

const Navbar = (props) => {
  return (
    <nav
      style={{
        width: "100vw",
        background: "pink",
        height: "60px",
      }}
    >
      navbar
    </nav>
  );
};

export default Navbar;
